spring:
  datasource:
    url: jdbc:postgresql://postgres.railway.internal:5432/railway
    username: postgres
    password: kwljoiVYavNGoMdomqmExaqoDqZNSDhV
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  web:
    application:
      name: api-rest-spring-boot-3

server:
  port: 8080
